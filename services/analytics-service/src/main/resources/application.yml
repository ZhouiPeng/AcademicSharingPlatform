spring:
  application:
    name: analytics-service
server:
  port: 8084

achievement:
  service:
    url: http://localhost:8082

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui/index.html

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:124.70.81.32}:${DB_PORT:3306}/${DB_NAME:analysis_service}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
    username: ${DB_USERNAME:buaa}
    password: "${DB_PASSWORD:Password@#@114514}"
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
