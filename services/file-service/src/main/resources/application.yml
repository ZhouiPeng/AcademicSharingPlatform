server:
  port: 8083

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui/index.html

obs:
  endpoint: ${OBS_ENDPOINT:https://obs.cn-north-4.myhuaweicloud.com}
  accessKey: ${OBS_ACCESS_KEY:}
  secretKey: ${OBS_SECRET_KEY:}
  bucket: ${OBS_BUCKET:team13-file}

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:124.70.81.32}:${DB_PORT:3306}/${DB_NAME:file_service}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
    username: ${DB_USERNAME:buaa}
    password: "${DB_PASSWORD:Password@#@114514}"
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration