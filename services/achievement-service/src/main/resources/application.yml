spring:
  application:
    name: achievement-service
  datasource:
    url: jdbc:mysql://124.70.81.32:3306/achievement_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
    username: ${DB_USERNAME:buaa}
    password: "${DB_PASSWORD:Password@#@114514}"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate.format_sql: true
server:
  port: 8082

springdoc:
  swagger-ui:
    enabled: false
