spring:
  datasource:
    url: jdbc:mysql://localhost:3306/achievementdb?useSSL=false&serverTimezone=UTC&characterEncoding=utf8mb4
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: INFO
    com.academic: INFO
