# 一、通用接口约束（所有接口必须遵守）
## 1.1 接口风格
遵循 RESTful 规范：GET=查询、POST=创建、PUT=修改、DELETE=删除

## 1.2 统一响应格式
```json
{
  "code": 1, // 1为成功，0（默认）及其他为失败（具体错误码按接口定义）
  "msg": "操作成功", // 成功时提示信息，失败时返回具体的错误提示
  "data": {}, // 业务数据（根据接口返回具体结构）
  "timestamp": 1699999999999 // 服务器时间戳（毫秒）
}
```

> 说明：原文中将 `code` 作为 HTTP 状态码的做法已统一为业务码，HTTP 状态仍可通过响应头返回（如 200/400/500），业务侧以 `code` 字段判断具体业务成功/失败。

## 1.3 分页参数统一
URL 查询参数统一采用 `pageNum` 与 `pageSize`，默认 `pageNum=1`，`pageSize=10`。例如：
```
?pageNum=1&pageSize=10
```

## 1.4 枚举值统一
以下为项目常用枚举示例（统一放在第一章，后续接口使用时引用编号）：
```json
{
  "roleType": { "1": "科研人员", "2": "普通用户", "3": "管理员" },
  "achievementType": { "1": "期刊论文", "2": "专利", "3": "科研项目", "4": "奖项" },
  "privacyLevel": { "1": "公开", "2": "机构内可见", "3": "私有" },
  "classification": {
    "100": {
      "name": "计算机科学",
      "sub": {
        "1001": "人工智能",
        "1002": "计算机视觉",
        "1003": "自然语言处理",
        "1004": "机器学习"
      }
    },
    "200": {
      "name": "电子信息",
      "sub": {
        "2001": "通信与信息系统",
        "2002": "信号与信息处理",
        "2003": "微电子学"
      }
    },
    "300": {
      "name": "材料科学",
      "sub": {
        "3001": "纳米材料",
        "3002": "能源材料",
        "3003": "复合材料"
      }
    }
  }
}
```

> 说明：`classification` 为分层枚举，第一层为学科大类（例如 100 为计算机科学），`sub` 为二级学科（例如 1001=人工智能）。如果需要更多学科，请在此处补充并同步到数据库/前端字典。

## 1.5 权限传递
默认所有接口均需在请求头携带：
```
Authorization: Token
```
（`{Token}`：授权令牌，类型：String，通常为 JWT；示例：`eyJhbGci...`）
特殊不需要 Token 的接口会在具体接口说明中明确标注。

## 1.6 文件交互规则
非结构化文件（PDF/头像等）在后端仅使用对象存储（例如华为云OBS）返回的 `fileId` 作为文件唯一标识，**不直接传递文件流**。

因此所有的请求头和返回头的 `Content-Type` 均应为 `application/json`（文件上传/下载由独立的存储服务或预签名 URL 处理）。

## 1.7 数据修改规则
涉及数据修改的 `PUT` 接口只需传入修改对象主键（`*Id`）以及 `data` 字段，`data` 中记录要修改的键名与修改内容，最小化传输体积。例如：
```json
{
  "achievementId": "ach001",//用户除外，用户可以从Token里取，直接把data的{}拿到最外层即可，详见2.2.6
  "data": { "title": "新的标题", "privacyLevel": 2 }
}
```

---

## 二、按功能详细接口说明（按模块与功能归类，排除内部调用接口）

说明：下面每个接口均遵循第 1 章的通用约束。路径中含 `{}` 为路径参数，分页使用 `pageNum` / `pageSize`。

**路径参数与占位符说明（已展开）**
- `{Token}` / `{token}`：授权令牌（JWT），类型：String，示例：`eyJhbGci...`。用于请求头 `Authorization: Bearer {Token}`（注意大小写与实现一致）。
- `{userId}`：用户唯一标识，类型：String，示例：`23373268`。用于标识用户的路径参数或请求体字段。
- `{authorname}`：作者用户名或别名，类型：String，示例：`zhangsan`。用于作者页路径 `/api/users/{authorname}`。
- `{achId}` / `{achievementId}`：成果唯一标识，类型：String，示例：`ach-2025001`。用于成果相关路径（查看/修改/删除/下载）。
- `{authorId}`：作者 ID，类型：String，示例：`author-123`。用于按作者查询成果的路径或参数。
- `{scholarId}`：学者/被关注者的 ID，类型：String，示例：`sch-456`。用于关注/取消关注接口路径。
- `{folderId}`：收藏夹 ID，类型：String，示例：`fld-001`。用于收藏相关路径。
- `{catId}`：分类 ID（category id），类型：Number 或 String，示例：`100`（计算机科学）。用于分类浏览路径。
- `{reportId}`：报告或举报记录的 ID，类型：String，示例：`rpt-001` / `rep-001`。用于导出/查询报告或举报记录的路径。
- `{authenticationId}`：门户认领记录 ID，类型：String，示例：`auth-2025001`。用于认领审批等操作。

注：上面占位符在文档中以 `{name}` 形式出现，使用时替换为实际值（如在 URL 中使用请注意必要时进行 URL 编码）。

**模块：用户管理（User Management）**

### 2.1 门户认领（Portal Claim）
#### 2.1.1 生成门户认领（管理员创建）
- 接口：`POST /api/admin/authentications`
- 权限：管理员
##### 请求参数JSON
```json
{
  "userId": "23373268",     // String，必填：需要认领的用户ID
  "institution": "XX大学",  // String，必填：所属机构
  "certFileId": "obs-cert-123", // String，必填：实名认证材料的 fileId（对象存储）
  "notes": "附加说明，可选"   // String，可选
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "认领已创建，等待分配管理员处理",
  "data": {
    "authenticationId": "auth-2025001",
    "status": "PENDING"
  },
  "timestamp": 1699999999999
}
```

#### 2.1.2 获取门户认领列表（管理员视角）
- 接口：`GET /api/admin/authentications`
- 权限：管理员
##### 请求参数（Query）
```
?pageNum=1&pageSize=10&status=PENDING
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "成功",
  "data": {
    "total": 123,
    "items": [
      { "authenticationId": "auth-2025001", "userId": "23373268", "status": "PENDING" }
    ]
  },
  "timestamp": 1699999999999
}
```

#### 2.1.3 处理门户认领（管理员审批）
- 接口：`PUT /api/admin/authentications`
- 权限：管理员
##### 请求参数JSON
```json
{
  "authenticationId": "auth-2025001", // String，必填
  "action": "APPROVE",                 // String，必填：APPROVE / REJECT
  "comment": "审核通过，已完成绑定"     // String，可选
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "处理成功",
  "data": null,
  "timestamp": 1699999999999
}
```

#### 2.1.4 前端查询当前用户的认领记录
- 接口：`GET /api/admin/getAuthentication`
- 权限：用户（或管理员）
##### 请求参数（Query）
```
?userId=23373268
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "成功",
  "data": {
    "authenticationId": "auth-2025001",
    "status": "PENDING",
    "submittedAt": 1699990000000
  },
  "timestamp": 1699999999999
}
```

---

### 2.2 用户注册 / 登录 / 资料（User auth & profile）

#### 2.2.1 普通用户注册
- 前置条件：已调用
- 接口：`POST /api/users/normal/register/{validateId}`
- 权限：匿名
##### 请求参数JSON
```json
{
  "username": "zhangsan@example.com", // String，必填
  "password": "P@ssw0rd",              // String，必填
  "email": "zhangsan@example.com",   // String，必填
  "displayName": "张三",             // String，可选
  "code": "134584"
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "注册成功",
  "data": { "userId": "23373268" },
  "timestamp": 1699999999999
}
```

#### 2.2.2 登录
- 接口：`POST /api/users/login`
- 权限：匿名
##### 请求参数JSON
```json
{
  "username": "zhangsan@example.com",
  "password": "P@ssw0rd"
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "登录成功",
  "data": {
    "token": "eyJhbGci...",      // String，JWT
    "expiresIn": 3600,            // 秒
    "user": { "userId": "23373268", "roleType": 2 }
  },
  "timestamp": 1699999999999
}
```

#### 2.2.3 退出登录

无需向后端发送请求，前端将本地存储的`token`删除即可.

#### 2.2.4 获取当前用户信息
- 接口：`GET /api/users/current`
- 权限：用户（需 Token）
##### 请求头
```
Authorization: token
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "获取成功",
  "data": {
    "userId": "23373268",
    "username": "Tom123",
    "displayName": "张三",
    "roleType": 2,
    "avatarFileId": "obs-file-123",
    "email": "123@123.com"
  },
  "timestamp": 1699999999999
}
```

#### 2.2.5 获取指定用户信息
- 接口：`GET /api/users/{userId}`（`{userId}`：用户唯一标识，String，示例：`23373268`）
- 权限：公开（部分字段给匿名用户）
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "成功",
  "data": {
    "userId": "23373268",
    "displayName": "张三",
  },
  "timestamp": 1699999999999
}
```

#### 2.2.6 修改当前用户信息
- 接口：`PUT /api/users/current`
- 权限：用户（需 Token）
##### 请求参数JSON（遵循第 1.7 最小修改原则）
```json
{
  "displayName": "张三2", 
  "avatarFileId": "obs-file-456",
  "email": "123@123.com"
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "更新成功",
  "data": {
      "userId": "123",
      "username": "Tom",
      "displayName": "张三2",
      "avatarFileId": "obs-file-456",
      "email": "123@123.com"
  },
  "timestamp": 1699999999999
}
```

#### 2.2.7 发送验证码

- 接口：`POST /api/users/verification/send`
- 使用场景：在修改密码前先调用该接口获取验证码
##### 请求参数JSON
- 在注册时email填具体值；重置密码时填`null`,此时要携带`token`
```json
{
  "email": "zhangsan@example.com"
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "重置验证码已发送",
  "data": {"validateId": "4523"},
  "timestamp": 1699999999999
}
```

#### 2.2.8 重置密码
- 前置条件：已调用2.2.7获取验证码和`validateId`
- 接口：`POST /api/users/password/reset/{validateId}`
- 权限：用户(需要Token)
##### 请求体

```json
{
  "password": "new_password",
  "code": "183493"
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1699999999999
}
```

#### 2.2.9 关注 / 取消关注 / 列表
- 接口：`POST /api/users/follow/{userId}`（关注）（`{uesrId}`：被关注用户ID，String）
- 接口：`DELETE /api/users/follow/{userId}`（取消关注）（`{userId}`：被关注用户ID，String）
- 接口：`GET /api/users/follows`（查看关注列表）
- 接口：`GET /api/users/fans`（查看粉丝列表）
##### 请求头
```
Authorization: token
```
##### 关注与取消关注返回数据JSON
```json
{
  "code": 1,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1699999999999
}
```

##### 查看列表返回数据JSON

==若不携带url参数默认是1.3的默认参数==

```json
{
    "code": 1,
    "msg": "查看成功",
    "data": 
    	{
            "total": 167,//记录总数
            "current":1,//当前页
            "pages":17,//总页数
            "size": 10,//页大小
            "records": [
                {
                    "userId": "23373268",
                    "username": "Tom123",
                    "displayName": "张三",
                    "roleType": 2,
                    "avatarFileId": "obs-file-123",
                    "email": "123@123.com"
                },
                {
                    "userId": "23373268",
                    "username": "Tom123",
                    "displayName": "张三",
                    "roleType": 2,
                    "avatarFileId": "obs-file-123",
                    "email": "123@123.com"
                },
                ...
            ]
    	}
}
```

#### 2.2.10 获取用户列表

- 接口：`GET /api/users`

##### 关注与取消关注返回数据JSON

```json
{
  "code": 1,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1699999999999
}
```

##### 查看列表返回数据JSON

==若不携带url参数默认是1.3的默认参数==

```json
{
    "code": 1,
    "msg": "查看成功",
    "data": 
    	{
            "total": 167,//记录总数
            "current":1,//当前页
            "pages":17,//总页数
            "size": 10,//页大小
            "records": [
                {
                    "userId": "23373268",
                    "username": "Tom123",
                    "displayName": "张三",
                    "roleType": 2,
                    "avatarFileId": "obs-file-123",
                    "email": "123@123.com"
                },
                {
                    "userId": "23373268",
                    "username": "Tom123",
                    "displayName": "张三",
                    "roleType": 2,
                    "avatarFileId": "obs-file-123",
                    "email": "123@123.com"
                },
                ...
            ]
    	}
}
```



**模块：学术成果（Achievements）**

### 2.3 成果 CRUD 与下载

#### 2.3.1 上传成果
- 接口：`POST /api/achievements`
- 权限：用户（需 Token）
##### 请求参数JSON
```json
{
  "userId": "23373268",        // 提交者ID，必填
  "title": "论文标题",          // String，必填
  "type": 1,                     // achievementType，枚举
  "authors": ["作者A","作者B"],
  "abstract": "摘要...",
  "fileId": "obs-file-789"      // 可选：全文或附件的 fileId
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "上传成功",
  "data": { "achievementId": "ach-2025001" },
  "timestamp": 1699999999999
}
```

#### 2.3.2 修改成果（部分字段）
- 接口：`PUT /api/achievements/{achId}`（`{achId}`：成果唯一标识，String，示例：`ach-2025001`）
- 权限：作者或管理员
##### 请求参数JSON（示例）
```json
{
  "achievementId": "ach-2025001",
  "data": { "title": "更新后的标题", "privacyLevel": 2 }
}
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1699999999999
}
```

#### 2.3.3 删除成果
- 接口：`DELETE /api/achievements/{achId}`（`{achId}`：成果唯一标识，String，示例：`ach-2025001`）
- 权限：作者或管理员
##### 返回数据JSON
```json
{ "code": 1, "msg": "删除成功", "data": null, "timestamp": 1699999999999 }
```

#### 2.3.4 浏览成果详情
- 接口：`GET /api/achievements/achievements/{achId}`（`{achId}`：成果唯一标识，String，示例：`ach-2025001`）
- 权限：公开（根据 privacyLevel 返回不同字段）
##### 返回数据JSON（示例）
```json
{
  "code": 1,
  "msg": "成功",
  "data": {
    "achievementId": "ach-2025001",
    "title": "论文标题",
    "authors": ["作者A"],
    "abstract": "...",
    "fileId": "obs-file-789",
    "createdAt": 1699980000000
  },
  "timestamp": 1699999999999
}
```

#### 2.3.5 某作者成果列表
- 接口：`GET /api/achievements/author/{authorId}`（`{authorId}`：作者 ID，String，示例：`author-123`）
- 权限：公开
##### 请求参数（Query）
```
?pageNum=1&pageSize=10
```
##### 返回数据JSON（示例）
```json
{
  "code": 1,
  "msg": "成功",
  "data": { "total": 12, "items": [ { "achievementId": "ach-...", "title": "..." } ] },
  "timestamp": 1699999999999
}
```

#### 2.3.6 下载成果全文
- 接口：`GET /api/achievements/{achId}/download`（`{achId}`：成果唯一标识，String，示例：`ach-2025001`）
- 权限：取决于 `privacyLevel`（可能需 Token）
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "成功",
  "data": { "downloadUrl": "https://...", "expiresAt": 1699993600000 },
  "timestamp": 1699999999999
}
```

---

### 2.4 收藏与查看

#### 新建收藏夹
- 接口：`POST /api/achievements/folders`
- 权限：用户
##### 请求参数JSON
```json
{ "name": "我的收藏", "description": "常用论文" }
```
##### 返回数据JSON
```json
{ "code": 1, "msg": "创建成功", "data": { "folderId": "fld-001" }, "timestamp": 1699999999999 }
```

#### 收藏 / 取消收藏
- 接口：`POST /api/achievements/{achId}/collect/{folderId}`（`{achId}`：成果ID，示例：`ach-2025001`；`{folderId}`：收藏夹ID，示例：`fld-001`）
- 接口：`DELETE /api/achievements/{achId}/collect`（`{achId}`：成果ID，示例：`ach-2025001`）
##### 返回数据JSON
```json
{ "code": 1, "msg": "操作成功", "data": null, "timestamp": 1699999999999 }
```

#### 删除收藏夹 / 列表
- 接口：`DELETE /api/achievements/collect/{folderId}`（`{folderId}`：收藏夹ID，示例：`fld-001`）
- 接口：`GET /api/achievements/collections`
##### 返回数据JSON（列表示例）
```json
{ "code":1, "msg":"成功", "data": [ { "folderId":"fld-001", "name":"我的收藏" } ], "timestamp":1699999999999 }
```

---

### 2.5 检索与筛选

#### 关键词搜索
- 接口：`GET /api/achievements/search`
- 权限：公开
##### 请求参数（Query）
```
?q=深度学习&pageNum=1&pageSize=10
```
##### 返回数据JSON
```json
{ "code":1, "msg":"成功", "data": { "total": 200, "items": [ { "achievementId":"...","title":"..." } ] }, "timestamp":1699999999999 }
```

#### 高级筛选
- 接口：`GET /api/achievements/filter` （若筛选条件复杂，建议变更为 `POST` 并提交 JSON 查询体）
##### 请求参数（Query 或 POST body）示例
```json
{
  "keywords": "机器学习",
  "classification": 100,
  "fromYear": 2018,
  "toYear": 2024,
  "pageNum": 1,
  "pageSize": 20
}
```

#### 分类浏览 / 排序
- 接口：`GET /api/achievements/category/{catId}`（`{catId}`：分类 ID，Number 或 String；示例：`100` 表示“计算机科学”）
- 接口：`GET /api/achievements/search/sort`（接受 `sortBy` 参数，如 `date` / `citations`）

---

**模块：统计分析（Analytics）**

### 2.6 热点分析 / 报告 / 统计

#### 分析科研领域热点
- 接口：`GET /api/analysis/hot-topics`
- 权限：公开或内部（视设计）
##### 请求参数（Query）
```
?classification=100&from=2023-01-01&to=2025-01-01&pageNum=1&pageSize=20
```
##### 返回数据JSON
```json
{
  "code": 1,
  "msg": "成功",
  "data": {
    "items": [ { "topic": "深度学习", "score": 98.6 }, { "topic": "联邦学习", "score": 75.2 } ]
  },
  "timestamp": 1699999999999
}
```

#### 导出分析报告
- 接口：`GET /api/analysis/report/{reportId}`（`{reportId}`：分析报告 ID，String，示例：`rpt-001`）
- 权限：用户或管理员（视权限设计）
##### 返回数据JSON
```json
{ "code":1, "msg":"成功", "data": { "reportId":"rpt-001", "downloadUrl":"https://..." }, "timestamp":1699999999999 }
```

#### 成果数据统计
- 接口：`GET /api/analysis/achievements`
- 权限：公开/管理员（视场景）
##### 请求参数（Query）
```
?groupBy=year&pageNum=1&pageSize=10
```
##### 返回数据JSON（示例）
```json
{ "code":1, "msg":"成功", "data": { "yearly": [ { "year":2024, "count":120 } ] }, "timestamp":1699999999999 }
```

---

**模块：系统管理（Admin / Config / Logs / 报告与处理）**

### 2.7 系统配置与日志

#### 修改系统参数
- 接口：`PUT /api/admin/configs`
- 权限：管理员
##### 请求参数JSON
```json
{ "key": "site.maintenance", "value": "true" }
```
##### 返回数据JSON
```json
{ "code":1, "msg":"更新成功", "data": null, "timestamp":1699999999999 }
```

#### 查看系统操作日志
- 接口：`GET /api/admin/logs`
- 权限：管理员
##### 请求参数（Query）
```
?pageNum=1&pageSize=50&level=ERROR
```
##### 返回数据JSON
```json
{ "code":1, "msg":"成功", "data": { "total": 100, "items": [ { "id":"log-1", "message":"..." } ] }, "timestamp":1699999999999 }
```

---

**模块：举报处理（Reports）**

### 2.8 举报生成与处理

#### 生成举报
- 接口：`POST /api/admin/report`
- 权限：匿名提交或登录用户
##### 请求参数JSON
```json
{ "reporterId": "2337", "targetUserId": "9999", "reason": "抄袭", "evidence": ["obs-file-..."] }
```
##### 返回数据JSON
```json
{ "code":1, "msg":"举报已创建", "data": { "reportId":"rep-001" }, "timestamp":1699999999999 }
```

#### 获取举报 / 处理举报
- 接口：`GET /api/admin/report` / `PUT /api/admin/report`
- 权限：管理员

#### 查询举报结果（前端用）
- 接口：`GET /api/admin/getReport`
- 权限：用户/管理员
##### 请求参数（Query）
```
?userId=2337
```
##### 返回数据JSON
```json
{ "code":1, "msg":"成功", "data": { "reportId":"rep-001", "status":"RESOLVED" }, "timestamp":1699999999999 }
```

---

## 四、说明与建议

- 对于复杂的筛选/分析查询，建议使用 `POST` 并在接口文档中明确查询体结构，避免 URL 长度限制和编码问题。
- 内部调用接口（例如 `/internal/files/*`、`/api/achievements/uncertified`、数据同步接口）已从此“对外/前端”文档中移除；如需单独维护内部开发文档，我可以为后端/运维生成一份 `internal-接口.md`。

